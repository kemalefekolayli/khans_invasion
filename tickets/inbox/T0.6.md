# TICKET: T0.6 — SelectableGeneral: non-alloc physics queries (kill per-frame allocations)

## Context
`SelectableGeneral` (Assets\code\Chars\InputMechanics\SelectableGeneral.cs) while selected runs per frame:
- `Update()` → `HandleInput()`, `CheckCurrentProvince()`, `CheckCityCenter()` (lines 147-169)
- `CheckCurrentProvince` / `CheckCityCenter` call `Physics2D.OverlapPointAll(transform.position)` (lines 343, 377) — each allocates a fresh `Collider2D[]` and sweeps against ALL colliders (176 province PolygonCollider2D + armies + generals)
- `IsPositionBlocked` (line 415) `OverlapPointAll` in `FixedUpdate` while moving

Per frame while a general is selected: up to 3 array allocations + 3 unfiltered physics sweeps. Also re-queries every frame even when the general isn't moving.

## Must Have
- [ ] Replace `OverlapPointAll` with the non-alloc overload using a reusable buffer + `ContactFilter2D` restricted to the relevant layers (provinces, city centers), e.g. `Physics2D.OverlapPoint(position, contactFilter, reusableBuffer)`.
- [ ] Reuse one buffer (`Collider2D[]` field) across the province/city-center checks.
- [ ] Skip re-scanning when the general's position hasn't moved beyond a small threshold since the last scan (moving generals re-scan; idle generals don't).
- [ ] No visible behavior change: province enter/exit and city-center detection still fire the same events.

## Done When
- [ ] Code review confirms no `new` allocation in the per-frame physics path (buffer reused, contact filter cached)
- [ ] Code compiles cleanly
- [ ] Selecting/moving a general still detects provinces and city centers correctly

## Manual Check
YES — select Khan, walk into a city center, confirm the C-button still appears and province exits/enters still fire

## Files Touched
- `Assets/code/Chars/InputMechanics/SelectableGeneral.cs`

## Branch
`feature/t0.6-general-physics-nonalloc`

## Notes for the Agent
- ContactFilter2D setup: set `useTriggers`, layer mask, then cache it (it's a struct — build once).
- Be careful: `OverlapPoint` (non-alloc) returns int count. Process `count` results, not the full buffer.
- Anti-slop: no new managers, no dead code, no duplicated helpers.
