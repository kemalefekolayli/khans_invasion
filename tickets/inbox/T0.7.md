# TICKET: T0.7 — GameFontManager: kill nested FindObjectsByType scan

## Context
`GameFontManager.Update()` (Assets\code\UI\GameFontManager.cs:82-88) runs a scene-wide scan every 0.75s:
- `FindObjectsByType<TMP_Text>(FindObjectsSortMode.None)` over the whole scene (line 124)
- for each text not matching early, `IsCityNameText` calls `FindObjectsByType<ProvinceNameDisplay>` (line 200) — a NESTED full scene scan per text, plus `GetComponentInParent` walks

This is O(N×M) scene sweeps every 0.75s with array allocations. At scale (many TMP texts in UI + world), this is wasteful.

## Must Have
- [ ] Build the city-name text set ONCE (a `HashSet<TMP_Text>` populated when `ProvinceNameDisplay` texts register, or a single `FindObjectsByType<ProvinceNameDisplay>` collected once at scan init).
- [ ] `IsCityNameText` becomes a `HashSet.Contains` check — no nested scene scan per text.
- [ ] The scan interval (0.75s) stays as-is; no change to which texts get the font applied.

## Done When
- [ ] Code review confirms no `FindObjectsByType` inside the per-text loop
- [ ] Code compiles cleanly
- [ ] Fonts still get applied to all non-city-name text (visual behavior unchanged)

## Manual Check
YES — open the game, confirm all UI/world text still uses the correct font

## Files Touched
- `Assets/code/UI/GameFontManager.cs`
- Possibly `Assets/code/ProvinceSystem/Controller/ProvinceNameDisplay.cs` (to register its text in the set)

## Branch
`feature/t0.7-fontmanager-cache`

## Notes for the Agent
- Read GameFontManager.cs fully first. Understand `scanInterval`, `ApplyToAllText`, `IsCityNameText`, `ApplyTo`.
- Anti-slop: no new parallel font system; the cache is a field inside GameFontManager (or a static registry populated by ProvinceNameDisplay). No dead members.
