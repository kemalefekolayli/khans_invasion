# TICKET: T0.4 — Fog of War: stop scanning all provinces every frame

## Context
`FogOfWarManager.Update()` (Assets\code\ProvinceSystem\Fog\FogOfWarManager.cs:230) iterates the ENTIRE `provinceFogStates` dictionary (~176 entries) EVERY frame, reading each province's `SpriteRenderer.color` and doing a `Color !=` compare. Most provinces are at steady state; only provinces mid-reveal or border-peek actually need per-frame lerping. This is a flat per-frame cost that scales with province count.

## Must Have
- [ ] Track only the provinces that are actively lerping (revealing / border-peek) in a set (e.g. `HashSet<ProvinceModel>` or a "dirty" flag).
- [ ] `Update()` iterates ONLY the active set, and removes a province from the set once its color reaches the target (epsilon compare).
- [ ] Entering the set happens where reveal/peek starts (province entered, border peek, owner change).
- [ ] No change to visible behavior — fog still reveals/lerps exactly as before.

## Done When
- [ ] Code review confirms `Update()` no longer iterates all 176 provinces every frame in steady state
- [ ] Code compiles cleanly (no syntax errors, all referenced types exist)
- [ ] Fog reveal/peek behavior is unchanged (same thresholds, same lerp speeds)

## Manual Check
YES — play, move a general into fogged territory, confirm fog still reveals smoothly

## Files Touched
- `Assets/code/ProvinceSystem/Fog/FogOfWarManager.cs`

## Branch
`feature/t0.4-fog-active-set`

## Notes for the Agent
- Read FogOfWarManager.cs fully first; understand `FogState`, `isRevealing`, `borderPeek`, `revealSpeed`.
- Anti-slop: do not add new managers or parallel fog logic; the set is a field inside FogOfWarManager. No dead members.
