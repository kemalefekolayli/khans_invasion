# TICKET: T0.1 — Consolidate Git Repos & Set Up Versioning (main / prod / feature)

## Context
The versioning setup is fragmented and must be consolidated into ONE repo at the project root:
- The project root `d:\khans_invasion\khans_invasion_v1` is NOT a git repo.
- `Assets\code` IS a git repo (on `master`, remote `origin` → `https://github.com/kemalefekolayli/khans_invasion.git`, head `e20294c`). It has a **dirty working tree** (~79 modified + many untracked files — this is the current in-progress work that MUST be preserved).
- `Code\` (root, capital C) is ANOTHER git repo but contains only a stale `sc.cs.meta` — junk.
- Scenes, prefabs, quests, and all other Assets are currently tracked nowhere.
- `tickets/` and `.opencode/` must be tracked.

Goal: a single git repo at the project root, correct Unity `.gitignore`, `main` + `prod` branches, remote connected, everything committed, and the remote pushed.

## Must Have
- [ ] Back up the nested repos WITHOUT deleting any project files: move `Assets\code\.git` and `Code\.git` to a backup folder OUTSIDE the project (e.g. `C:\Users\kemal\AppData\Local\Temp\opencode\khans_backup\`). Do this by moving, not deleting, so nothing is lost.
- [ ] `git init` in the project root `d:\khans_invasion\khans_invasion_v1`
- [ ] Create a proper Unity `.gitignore` at the root covering at least:
      `Library/`, `Temp/`, `obj/`, `Logs/`, `UserSettings/`, `*.csproj`, `*.sln`, `replay_*.log`, `*.user`, `Build/`, `Builds/`, `.idea/`, `.vscode/`, `*.pidb`, `*.booproj`, `*.svd`, `*.csproj.meta`, `*.sln.meta`
      Do NOT ignore `Assets/`, `Packages/`, `tickets/`, `.opencode/`, `ProjectSettings/`.
- [ ] Rename the default branch to `main` (e.g. `git branch -M main`).
- [ ] Add remote `origin` → `https://github.com/kemalefekolayli/khans_invasion.git`
- [ ] Initial commit on `main` containing the ENTIRE project (all Assets, code, tickets, .opencode).
- [ ] Create `prod` branch from `main`.
- [ ] Push to the remote: `git push -u origin main` and `git push -u origin prod`. Use `$env:GIT_TERMINAL_PROMPT=0` so git fails fast instead of hanging on credentials. If push fails due to auth/network, report `Status: PARTIAL` with the local repo fully set up and note the push is pending (do NOT retry, do NOT force-push).

## Nice to Have
- [ ] A `docs/` folder with the ticket format + workflow doc (so future agents know the conventions)

## Done When
- [ ] `git -C <root> log --oneline -1` shows an initial commit on `main`
- [ ] `git -C <root> branch -a` shows `main` and `prod`
- [ ] `git -C <root> remote -v` shows the GitHub remote
- [ ] `git -C <root> status` is clean (nothing untracked except gitignored build/temp dirs)
- [ ] `Assets\code\.git` and `Code\.git` no longer exist in the project (moved to backup)
- [ ] Push to the remote succeeds (or is explicitly reported as pending due to credentials)

## Manual Check
NO — all conditions verifiable via git commands

## Files Touched
- `.gitignore` (new, at project root)
- Repo metadata only — NO source files changed, NO files deleted

## Branch
`main` (this is the base; create `prod` alongside)

## Notes for the Agent
- Windows / PowerShell. `&&` does NOT work — use `;` to chain commands.
- CRITICAL: The dirty work in `Assets\code` (the ~79 modified + untracked files) is the user's recent work. After consolidation it must be present in the initial commit. Do NOT delete, revert, stash, or clean anything.
- `Code\sc.cs.meta` is junk; keep the file (it will be committed as part of Assets) but its `.git` folder gets backed up like the rest.
- After moving `Assets\code\.git`, run `git -C <root> status` and confirm the expected files appear as untracked before committing.
- Verify push with a timeout; on any auth prompt, kill it and report PARTIAL.
