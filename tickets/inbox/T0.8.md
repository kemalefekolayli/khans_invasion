# TICKET: T0.8 — ArmyFlagVisuals color caching + minor per-frame allocations

## Context
Two categories:

**A) ArmyFlagVisuals** (Assets\code\Chars\WarMechanics\ArmyLogic\ArmyFlagVisuals.cs:49-58, 165-171): `LateUpdate()` calls `GetOwnerColor(owner)` for EVERY army EVERY frame, which calls `NationLoader.HexToColor(owner.nationColor)` — a hex string parse + allocation per army per frame — even though the flag only needs refreshing when the owner/color changes. Steady state should be a pointer compare.

**B) Minor per-frame allocations** (fix only if trivial and safe):
- `GeneralSelectionInput` (Chars\InputMechanics\GeneralSelectionInput.cs:70-75) allocates `new Key[] { Key.Digit1, ... }` every frame → hoist to a static readonly field.
- `PlayerNationGUI.GetKhanGeneral` (UI\TopLeft\PlayerNationGUI.cs:304) and `RaidButton`/`EnemyProvinceButton` `GetCurrentRaider` (RaidButton.cs:209, EnemyProvinceButton.cs:343) use `FindObjectsByType<SelectableGeneral>` fallbacks on events → replace with `GeneralSelectionManager`'s registered-generals list if one exists; otherwise leave them (event-driven, not per-frame) and note it.

## Must Have
- [ ] `ArmyFlagVisuals.LateUpdate` short-circuits: only refresh when `owner` reference changed OR cached color changed. Cache the parsed `Color` per `NationModel` (e.g. a small static/instance dictionary keyed by nation) so the hex is parsed once per nation, not per army per frame.
- [ ] `GeneralSelectionInput` key array is a `static readonly` field (no per-frame allocation).
- [ ] No visible change: flags still show the correct nation color and refresh on conquest.

## Nice to Have
- [ ] Replace the `FindObjectsByType<SelectableGeneral>` fallbacks in PlayerNationGUI/RaidButton/EnemyProvinceButton with a cached list if `GeneralSelectionManager` exposes one (check `RegisteredGenerals`).

## Done When
- [ ] Code review confirms the per-frame hex-parse is gone (or cached per nation)
- [ ] Code review confirms no `new` array allocation in `GeneralSelectionInput.Update`
- [ ] Code compiles cleanly

## Manual Check
YES — start the game, confirm army flags show correct nation colors, and conquest recolors flags

## Files Touched
- `Assets/code/Chars/WarMechanics/ArmyLogic/ArmyFlagVisuals.cs`
- `Assets/code/Chars/InputMechanics/GeneralSelectionInput.cs`
- (Nice to Have) `Assets/code/UI/TopLeft/PlayerNationGUI.cs`, `Assets/code/UI/ProvButton/RaidButton.cs`, `Assets/code/UI/ProvButton/EnemyProvinceButton.cs`

## Branch
`feature/t0.8-flag-cache-minor-alloc`

## Notes for the Agent
- If `NationLoader.HexToColor` already has a cache, reuse it; don't build a second one.
- Anti-slop: keep the diff small and targeted. If any part of (B) risks a behavior change, leave it and say so in the result.
