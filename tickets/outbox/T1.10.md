# RESULT: T1.10 — Quest state invariants and dynamic value targets

## Status
DONE

## Summary
QuestManager now resets all runtime quest collections on initialization, gates progress and value checks on prerequisite completion, and enforces prerequisite claims in the manager. Value quest targets are computed at runtime from settled starting values using serialized multiplier/margin settings; QuestItemUI uses the effective target and manager claimability rule.

## Verification
- [x] Fresh state clears progress, completed, and claimed collections; root quests remain unlocked by the existing `prerequisiteQuestId < 0` rule.
- [x] Locked action and value quests are skipped until their prerequisite is completed. Action events are evaluated against the unlock state at the start of the event.
- [x] `TryClaimQuest` calls the manager-owned `CanClaimQuest` rule, which requires a completed quest and a claimed prerequisite.
- [x] QuestItemUI interactability is driven only by `CanClaimQuest`; completed-but-not-claimable quests remain disabled.
- [x] Effective targets use `max(baseline + margin, ceil(startValue * multiplier + margin))`, with defaults of multiplier `1.25` and margin `1`. Initialization waits for gold, income, population, and ArmyManager values to remain stable, including delayed army-spawn changes.
- [x] QuestManager scripts recompiled through Unity MCP: successful compilation, with five pre-existing warnings in unrelated files and no errors.
- [x] Unity EditMode test runner completed with no project tests discovered and no failures.
- [x] `git diff --check` completed without whitespace errors; forbidden StartScene, Packages, ProjectSettings, MCP, and QuestData asset files were not changed by this ticket.

## Manual Check
YES — Launch the game, open the quest panel, confirm only the root quest is active initially, verify locked children do not progress or claim, and confirm value quest targets are above the observed starting values (gold 12333, income 184, population 813, army 150). Complete and claim a prerequisite, then verify its child becomes progressable and only becomes claimable after the prerequisite is claimed.

## Files Changed
- Assets/code/QuestSystem/QuestManager.cs
- Assets/code/QuestSystem/QuestItemUI.cs
- tickets/outbox/T1.10.md

## Branch
feature/t1.10-quest-state-dynamic-targets

## Notes / Issues
QuestTooltip was intentionally not modified per the ticket-owner instruction; `QuestManager.GetEffectiveTarget(int)` is the runtime accessor available to the UI agent. Existing unrelated worktree changes, including StartScene and package changes, were preserved and are not part of this ticket commit.
