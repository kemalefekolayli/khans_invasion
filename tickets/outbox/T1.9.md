# RESULT: T1.9 — Quest UI interaction and layout correction

## Status
PARTIAL

## Summary
Updated the quest prefab with a scale-safe canvas, compact readable tooltip layout, 8px scrollbar handles, and transparent 16px pointer hit areas that remain draggable. Quest UI raycasting/ScrollRect setup now supports wheel and drag interaction, while CameraController ignores UI input so quest scrolling cannot zoom or pan the world. QuestTooltip now converts against its actual parent canvas and clamps its full rect to the canvas edges.

## Verification
- [x] Scrollbar handles are 8px wide/high, with transparent 16px track hit areas; ScrollRect remains permanent/visible and has valid built-in UI component references.
- [x] `rowSpacing` remains 140 and the tree layout code was not changed.
- [x] QuestPanelCanvas root scale is `{x: 1, y: 1, z: 1}` rather than zero.
- [x] `git diff --check` passes; changed paths are limited to the allowed prefab and two UI/input scripts.
- [ ] Exact isolated-worktree Unity compilation (the MCP server is attached to the original worktree) — the active project recompile reported 0 warnings, and the isolated branch changes were additionally reviewed for Unity API/type/reference correctness.

## Manual Check
YES — Open the quest panel in Unity, confirm vertical scrolling and scrollbar dragging work, verify the camera does not zoom or right-drag while the pointer is over the panel, and hover quests near each screen edge to confirm the tooltip stays readable and fully on-screen.

## Files Changed
- Assets/Resources/QuestPanelCanvas.prefab
- Assets/code/MapSystem/CameraController.cs
- Assets/code/QuestSystem/QuestTooltip.cs
- tickets/outbox/T1.9.md

## Branch
feature/t1.9-quest-ui-input

## Notes / Issues
The implementation was made in a clean worktree branched from `main` so unrelated changes in the original worktree were preserved. The active Unity MCP recompilation targets the original Unity project rather than this temporary worktree; no StartScene or project/package files were edited or saved. The planner/tester should run the normal Unity compile/test pass on this branch before merge.
