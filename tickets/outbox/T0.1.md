# RESULT: T0.1 — Consolidate Git Repos & Set Up Versioning (main / prod / feature)

## Status
DONE

## Summary
Consolidated the fragmented versioning into a single git repo at the project root. Backed up the nested `Assets\code\.git` and `Code\.git` repos to `C:\Users\kemal\AppData\Local\Temp\opencode\khans_backup\` (moved, nothing deleted), initialized a fresh repo on `main`, added a Unity `.gitignore`, committed the ENTIRE project (including the previously-untracked dirty work from `Assets\code`, all Assets, tickets, .opencode) as the initial baseline, created `prod`, and pushed both branches to the GitHub remote successfully.

## Verification
- [x] `git log --oneline -1` → `3cf1d69 chore: consolidate into single root repo with Unity .gitignore` (initial commit on `main`)
- [x] `git branch -a` shows `main` and `prod` (plus remotes/origin/main and remotes/origin/prod)
- [x] `git remote -v` shows `origin https://github.com/kemalefekolayli/khans_invasion.git`
- [x] `git status` is clean ("nothing to commit, working tree clean", on main, up to date with origin/main)
- [x] `Assets\code\.git` and `Code\.git` no longer exist in the project; both moved to backup (`assets_code_git\HEAD` and `code_git\HEAD` verified present)
- [x] Push to remote succeeded: `main -> main` and `prod -> prod` (EXIT=0 for both)

## Manual Check
NO

## Files Changed
- `.gitignore` (new, at project root)
- Repo metadata only (`.git/`) — NO source files modified, NO files deleted. Dirty work in `Assets\code` preserved in the initial commit.

## Branch
`main` (base) + `prod` (created alongside)

## Notes / Issues
- Moving the nested `.git` dirs initially failed with a "sufficient access rights" error: `Move-Item` copied the contents to the backup but could not remove the source directory. The source `.git` dirs were left as EMPTY directory shells (all contents already safely in the backup). I removed only the empty leftover `.git` shells — no project files were affected. Verified the backup contains the full `.git` contents (HEAD, index, objects, config, refs, etc.) for both repos.
- Committed via `git add -A`; ignored dirs (`Library/`, `Temp/`, `obj/`, `Logs/`, `UserSettings/`, `*.csproj`, `*.sln`, etc.) confirmed excluded.
- Both pushes completed with `$env:GIT_TERMINAL_PROMPT=0`; no auth issues encountered.
