# RESULT: T1.3 — Quest System Core Fixes (Firing Logic + Unlock Semantics + Rewards)

## Status
DONE

## Summary
Implemented all Must Have requirements for the quest system. `GameEvents.NationDestroyed` now fires in both conquest paths (`SiegeManager.ConquerProvince` and `AINationController.TransferProvince`) when a nation loses its last province. `QuestManager.OnArmyDefeated` no longer counts the player's own lost armies. Fixed quest asset data (Quest_1 targetCount=3, Quest_6 reward description matches MoveCapitalAbility), wired Quest_6 into GameScene's `QuestManager.allQuests`, routed the NewGeneral reward through `GeneralSpawner.SpawnQuestRewardGeneral` (removing the raw-Instantiate path and the broken `generalPrefab`/`generalSpawnPoint` fields), included `bonusTradeIncome` in `IncomeProcessor.ProcessTurnEnd`, and changed `IsQuestUnlocked` to use `completedQuests` instead of `claimedQuests`.

## Verification
- [x] `GameEvents.NationDestroyed(oldOwner)` fires in `SiegeManager.ConquerProvince` and `AINationController.TransferProvince` guarded by `oldOwner != null && oldOwner.provinceList != null && oldOwner.provinceList.Count == 0` (code review: `Assets/code/SiegeSystem/SiegeManager.cs:607-612`, `Assets/code/AI/AINationController.cs:585-590`)
- [x] `OnArmyDefeated` returns early when `army?.OwnerNation == PlayerNation.Instance?.Nation` (code review: `Assets/code/QuestSystem/QuestManager.cs:95-101`)
- [x] `Quest_1.asset` `targetCount: 3`; `Quest_6.asset` `rewardDescription: Unlock the Move Capital ability.` with `rewardAmount: 1` (rewardType 4 = MoveCapitalAbility)
- [x] `GameScene.unity` `QuestManager.allQuests` now includes GUID `a303f095fe2248c42ac336a5bb4132d7` (Quest_6) as the 6th entry
- [x] `GeneralSpawner.SpawnQuestRewardGeneral(string name, int armySize)` public method added, reusing `SpawnGeneral`/`SpawnArmyForGeneral`; QuestManager `SpawnNewGeneral` raw-Instantiate path removed; broken `generalPrefab`/`generalSpawnPoint` fields removed from QuestManager and GameScene
- [x] `IncomeProcessor.ProcessTurnEnd` uses `float tradeIncome = (playerNation.TradeIncome + playerNation.bonusTradeIncome) * tradeMultiplier;`
- [x] `IsQuestUnlocked` uses `completedQuests.Contains(quest.prerequisiteQuestId)`
- [x] No dead code: removed `generalPrefab`, `generalSpawnPoint`, `SpawnNewGeneral` (only referenced in the removed path); new methods all referenced
- [x] No new GameEvents added; reused existing `GameEvents`/`GameLog` patterns

## Manual Check
YES — verify in Unity (editor currently open; do NOT run batchmode):
1. Play a game, destroy an AI nation's last province via siege AND via AI transfer → `>> Event: NationDestroyed` in log, DestroyNation quest (Quest_6) completes.
2. Lose a battle as the player → DefeatArmies (Quest_4) progress does NOT increase; win a battle against AI → it does.
3. Quest_1 requires 3 raids; Quest_6 appears in the quest panel (wired into allQuests).
4. Claim the NewGeneral reward (Quest_4) → a proper general with an army spawns near the Khan (via GeneralSpawner).
5. Claim a TradeIncome reward → next turn's trade income increases.
6. Quest unlocks: child quest appears when parent is completed (before claiming).
7. Unity console clean — no missing-field warnings from the removed `generalPrefab`/`generalSpawnPoint` fields.

## Files Changed
- `Assets/code/QuestSystem/QuestManager.cs`
- `Assets/code/Chars/GeneralSpawner.cs`
- `Assets/code/TurnSystem/IncomeProcessor.cs`
- `Assets/code/SiegeSystem/SiegeManager.cs`
- `Assets/code/AI/AINationController.cs`
- `Assets/Quests/Quest_1.asset`
- `Assets/Quests/Quest_6.asset`
- `Assets/Scenes/GameScene.unity`

## Branch
`feature/t1.3-quest-core-fixes`

## Notes / Issues
- `Quest_6.rewardAmount` set to `1` to represent "1 ability unlocked" (MoveCapitalAbility ignores the amount in `ApplyReward`, but a non-zero value is more sensible than the previous `0`).
- `GeneralSpawner.SpawnQuestRewardGeneral` falls back to `startingArmySize` (50 in scene) when `armySize <= 0`. Quest_4 (NewGeneral) has `rewardAmount: 0`, so it uses the default size.
- `QuestManager` now locates `GeneralSpawner` via `FindFirstObjectByType` at reward time; GeneralSpawner is present in GameScene on the GeneralSelectionManager GameObject, so it resolves correctly. No scene wiring change needed beyond Quest_6.
- Verified by careful code review only (Unity editor is open; batchmode compile would conflict). Human must confirm in-game per Manual Check.
