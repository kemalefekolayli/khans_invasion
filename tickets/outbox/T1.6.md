# RESULT: T1.6 — Quest Tree UI (Scrollable Tree Panel)

## Status
DONE

## Summary
Reworked the quest panel from a fixed 5-slot horizontal row into a fully dynamic, scrollable TREE. `QuestPanelController` now builds one `QuestItemUI` per quest in `QuestManager.allQuests` (16 quests, no hardcoded slots), computes a proper tree layout from `prerequisiteQuestId` (leaves occupy columns, internal nodes centered over their children, depth = row), and draws runtime parent→child connector segments that brighten when the child unlocks. The `QuestPanelCanvas.prefab` was rebuilt around a masked `ScrollRect` (vertical + horizontal scrollbars) with a single inactive `QuestItem` template (box image + Title + Progress + Arrow) that is instantiated per quest. `QuestItemUI` now shows the real `questTitle` in MedievalSharp (via `GameFontManager.Apply`), shows `x/y` progress on active quests, uses 5 distinct state colors (locked/active/completed/claimable/claimed), and keeps hover tooltips. Connectors + item states refresh live via `OnQuestProgressUpdated`/`OnQuestCompleted`/`OnQuestClaimed`.

## Verification
- [x] Done When 1 — Opening the panel shows ALL quests in a scrollable tree. `QuestPanelController.BuildTree` (`Assets/code/QuestSystem/QuestPanelController.cs:97`) iterates `QuestManager.allQuests` (16), `AssignTreePositions` (line 256) walks `prerequisiteQuestId` relationships recursively, `ScrollRect` wraps the content, and `ResetScrollPosition` (line 407) resets scroll on open.
- [x] Done When 2 — Parent/child unlock relationships are visually clear: connectors drawn by `CreateConnectors`/`CreateConnectorSegment`/`AddConnectorRect` (lines 168-235) run between each parent and its children; `RecolorConnectors` (line 237) brightens them using `QuestManager.IsQuestUnlocked` (root = unlocked, child unlocked when parent completed). Root quest 0; children 1/6/9 at depth 1; max depth 5.
- [x] Done When 3 — Children unlock live when a parent completes: controller subscribes to `QuestManager.OnQuestProgressUpdated`/`OnQuestCompleted`/`OnQuestClaimed` in `OnEnable` (lines 57-66) and calls `RecolorConnectors` on any quest event; `QuestItemUI` independently re-runs `UpdateVisualState` on the same events (lines 50-73).
- [x] Done When 4 — No hardcoded 5-slot layout remains: prefab has only the template child under Content; `git grep "Quest_1"` on the prefab and code returns nothing. Adding a quest asset to `allQuests` automatically appears.
- [x] Must Have — Prefab rebuilt: `QuestPanelCanvas.prefab` now has ScrollView (ScrollRect guid `f70555f144d8491a825f0804e09c671c`) → Viewport (RectMask2D `2bda77f7339957c48bd8be0baf0a0f8c`) → Content → `QuestItem` template (SetActive 0, size 200x140) with QuestItemUI + Image + Button + Title/Progress/Arrow. Vertical + horizontal scrollbars wired (`a7938bc348d2566468a9b6d7783a0316`). YAML verified: 80 blocks, no duplicate/undefined fileIDs, all 20 GameObject↔RectTransform parent/child links consistent.
- [x] Must Have — Beautified: `QuestItemUI.cs` shows `questTitle`, applies `GameFontManager.Apply`, adds `progressText` field, 5 colors (locked 0.25 gray, active gold 0.95/0.85/0.4, completed gray, claimable green, claimed blue), CLAIM! on claimable (line 89), tooltip hover preserved.
- [x] Syntax/API review (code-only, see below) — verified all referenced APIs exist: `QuestManager.allQuests`, `GetQuestProgress`, `IsQuestUnlocked`, `IsQuestCompleted`, `IsQuestClaimed`, `TryClaimQuest`, events `OnQuestProgressUpdated`/`OnQuestCompleted`/`OnQuestClaimed` (QuestManager.cs:237-279); `GameEvents.OnProvincePanelClosed`/`ProvincePanelClosed`/`QuestPanelOpened`/`QuestPanelClosed` (GameEvents.cs:42/170/303/324); `GameFontManager.Apply` (GameFontManager.cs:134); `QuestTooltip.Instance.Show/Hide` (QuestTooltip.cs:6/50/60); `QuestPanelButton` still calls `QuestPanelController.Instance.TogglePanel()` (QuestPanelButton.cs:35).

## Manual Check
YES — Open the quest panel in Unity and confirm: (1) all 16 quests appear in a tree (root "Raid 3 Provinces" top-left, children branching below); (2) the tree scrolls both vertically and horizontally via the two scrollbars; (3) parent→child connector lines show, dim for locked children and brighten when the parent completes; (4) completing a parent quest live-unlocks the child (state + connector); (5) quest titles show real names (not "QUEST n") in MedievalSharp; (6) active quests show `x/y` progress; (7) hover shows the QuestTooltip; (8) clicking a claimable quest claims it (green CLAIM! → blue CLAIMED). Unity editor must import the rebuilt prefab (editor is open) — verify no serialization errors in console.

## Files Changed
- Modified: `Assets/code/QuestSystem/QuestPanelController.cs` (dynamic build + tree layout + runtime connectors + scroll reset)
- Modified: `Assets/code/QuestSystem/QuestItemUI.cs` (real title, progress text, 5-state colors, live refresh kept)
- Modified: `Assets/Resources/QuestPanelCanvas.prefab` (ScrollRect + Viewport + Content + QuestItem template + 2 scrollbars; removed hardcoded Quest_1..5 children; kept stable GUIDs for QuestPanel/TooltipPanel/root)

## Branch
`feature/t1.6-quest-tree-ui` (commit `9d9d80a`)

## Notes / Issues
- **Prefab hand-authored YAML** (editor is open, so Unity could not be used to build it). Verified programmatically: 80 YAML blocks, no duplicate or undefined local fileIDs, all parent/child references bidirectional. Sprite/texture external fileIDs (`6639126460929552292`, `8233180929103145980`, `2266019648972387503`) and Unity class/asset ids (11500000, 11400000, 2180264) are expected. Human MUST confirm Unity imports it cleanly.
- **Runtime connectors** chosen instead of prefab connector sprites: `QuestPanelController` draws thin `Image` rects between parent/child at build time, so no new connector textures/GameObjects in the prefab were needed.
- **Layout algorithm**: recursive leaf-column layout — each leaf takes a column, internal nodes center over their children's columns. Content size = leafCount×columnSpacing + padding wide, (maxDepth+1)×rowSpacing + padding tall (~1510×1240 at 200×140 nodes). A cycle/missing-prereq safety net places any unreachable quest (missing prereq treated as root).
- **QuestItem template stays a prefab child with SetActive false** and is skipped by `ClearQuestItems` (`child == questItemTemplate`, line 331).
- **New connector fields** (`lockedConnectorColor`, `unlockedConnectorColor`, `connectorThickness`) are not serialized in the prefab YAML; they fall back to the C# defaults in code, which is safe.
- **Unity compile NOT run** (editor is open, per instructions). Code reviewed against every referenced API; the human must do the in-game manual check above.
- Nice-to-haves (X button, drag-to-scroll) were NOT added — scope kept to Must Have; scrollbars satisfy the scrolling requirement.
- The T1.6 lock file (`tickets/inbox/T1.6.lock`) was created at start and left uncommitted (matching prior ticket convention); it can be removed on merge.
